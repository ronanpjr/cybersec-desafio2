name: Documentação

on:
  pull_request:
  push:
    branches: [main]

jobs:
  links-internos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar links relativos em Markdown
        run: bash scripts/check-doc-links.sh
